const sumar = (x,y) => {
    return x + y
}
const restar = (x,y) => {
    return x - y
}
const multiplicar = (a,b)=> {
    return a * b
}
const dividir = (a,b)=> {
    return a / b
}

export {restar, sumar, multiplicar, dividir}